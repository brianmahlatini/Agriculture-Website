// Footer keeps platform identity and stack signal available without crowding the main page.
export function Footer() {
  return (
    <footer className="footer">
      <div>
        <strong>Agricore</strong>
        <span>Enterprise agriculture for resilient food systems.</span>
      </div>
      <span>React | Node.js | MongoDB | PostgreSQL | Docker</span>
    </footer>
  );
}

