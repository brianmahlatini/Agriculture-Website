// Public API metadata is centralized so docs, root responses, and future versioning stay aligned.
export const apiResources = [
  '/api/health',
  '/api/operations/overview',
  '/api/content/impact',
  '/api/leads'
];

